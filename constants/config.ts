/**
 * App-wide configuration for TrueNorth.
 * Placeholder keys (e.g. apiUrl) reserved for future Supabase and env usage.
 */
export const config = {
  appName: 'TrueNorth',
  /** Reserved for Supabase project URL. */
  supabaseUrl: '' as string,
  /** Reserved for Supabase anon key. */
  supabaseAnonKey: '' as string,
} as const;
